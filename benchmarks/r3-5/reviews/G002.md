# R35-G002 — Independent Review

- Review date: 2026-09-12
- Reviewer: ChatGPT / GPT-5.6 Sol
- Research result reviewed: original research commit `07b809b37a556a85898b38194172f0f3b48ace83`
- Reconstructed PR head before review: `44ab7d11749d853258b1e97f5cb2dcfc6fc45249`
- Goal: `R35-G002`
- Discovery: `CONTAMINATED`

## Verdict

- `R35-C003`: `PROVEN × ACCEPTED`
- `R35-C004`: `PROVEN × ACCEPTED`
- `R35-G002`: `SOLVED` accepted
- Exact result: `R(3,5)=14` independently accepted within this benchmark

No mathematical gap remains in the submitted upper-bound proof.

## Independent proof audit

The proof is a complete degree split on an arbitrary vertex `v` of an arbitrary 14-vertex triangle-free graph `G`.

1. If `d(v) >= 5`, then `N(v)` is independent because an edge inside `N(v)` together with `v` would form a triangle. Hence any five vertices in `N(v)` form an independent 5-set.
2. If `d(v) <= 4`, then the non-neighborhood excluding `v` has size `|M(v)| = 13-d(v) >= 9`. The induced graph on `M(v)` is triangle-free. Stage 1 `C005`, already `PROVEN × ACCEPTED`, implies every triangle-free graph on at least nine vertices contains an independent 4-set. Adding `v`, which is nonadjacent to every vertex of `M(v)`, produces an independent 5-set.

The cases `d(v) >= 5` and `d(v) <= 4` exhaust all integer degrees. Therefore every 14-vertex graph contains either a triangle or an independent 5-set, which is exactly `R(3,5) <= 14`.

Combining this with the independently accepted lower bound `R35-C002`, namely `R(3,5) >= 14`, yields `R(3,5)=14`.

## Dependency audit

The submitted dependency DAG is acyclic:

```text
Stage 1 C003 -> Stage 1 C004 -> Stage 1 C005 -> R35-C003
R35-C001 -> R35-C002
R35-C002 + R35-C003 -> R35-C004
```

The upper-bound branch does not use the G001 heuristic failure at 14 vertices, its search log, or the lower-bound certificate.

## Boundary and case audit

- `d(v)=5` is correctly handled by the high-degree case.
- `d(v)=4` gives exactly nine nonneighbors and is correctly handled by the low-degree case.
- For `d(v)<4`, choosing any nine vertices from `M(v)` is sufficient for Stage 1 `C005`.
- The independent 4-set lies inside `M(v)`, so `v` is distinct from it and nonadjacent to all four vertices.
- No hidden WLOG or symmetry assumption is used.

## Contamination / provenance audit

The original Git history records additional recalled strategy before proof work:

1. `00b886b` — recalled Ramsey recurrence / neighborhood split recorded as contamination.
2. `36215af` — execution budget and fresh-session provenance recorded.
3. `07b809b` — self-contained upper-bound proof committed.
4. `5e9cc34` — final usage counters recorded.

Thus the contamination checkpoint is auditable and precedes the proof artifact. Discovery remains `CONTAMINATED`; mathematical correctness is unaffected.

## Accidental main push recovery

The original G002 commits were accidentally pushed directly to `main`. The recovery deliberately preserves that history:

- `b4625cf` restores the pre-G002 tree on `main` without rewriting history.
- PR #5 reconstructs the same G002 tree transitions on `codex/r35-g002-upper-bound` as new commits.

This operational recovery does not change the mathematical content under review. The original research commit hashes remain cited above as provenance.
