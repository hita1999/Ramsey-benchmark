# R(3,4) — 研究ノート

## 現在の状態

G001: `SOLVED`。8頂点の証明書から下界 R(3,4)≥9 を得た。
ベンチマーク全体は `PARTIAL_PROGRESS`。上界・正確な値・独立レビューは未完了。
数学的状態と独立レビュー状態を分離する。以下のPROVENは自足的証明の存在を示し、レビューで受理済みという意味ではない。

## Goal実行契約と結果

- Goal ID: G001
- 対象: `benchmarks/r3-4/problem.md`
- Base commit: `0ae91ffcc540b029f1486f2e32a78167fa83ed0d`
- 実行日: 2026-09-06
- 実行者: Codex / GPT-6（詳細モデル設定・reasoning effortは未記録）
- 手段: Python標準ライブラリによる一般的な頂点追加DFS。外部検索なし。
- 計算予算: 探索全体で候補マスク500,000個。再現確認で同じ探索を1回追加実行。
- Checkpoint方針: 予算到達時に最大の証明書と探索状態を保存し、検証・文書化してGitに固定。
- 探索停止: `EXHAUSTED_BUDGET`。証明書構成Goalの終了分類: `SOLVED`。
- 両状態が異なる理由: G001は最大性や探索完了を要求せず、保存された具体的証明書と決定的検証で達成するため。
- 探索時間: 初回ツール実行約0.56秒（起動等を含む参考値）。厳密なCPU時間は未計測。
- Token予算: 数値の指定なし。最終消費量はこのファイルでは未計測。

## Claim台帳

### C001

- 命題: 頂点0〜7、赤い辺03,05,14,16,25,26,27,36,37,47は赤三角形も青K4も含まない。
- 数学的状態: `PROVEN`
- 根拠: `proof.md` の近傍表と全独立三頂点集合の有限証明。`certificate.json` が機械可読証明書。
- 依存するClaim: なし。
- 機械検算: 全56三頂点集合・全70四頂点集合で違反なし。
- 独立検証: 未実施（レビュー待ち）。
- 導入したGoal: G001。

### C002

- 命題: R(3,4)≥9。
- 数学的状態: `PROVEN`
- 根拠: C001の8頂点回避彩色とRamsey数の定義。`proof.md` 参照。
- 依存するClaim: C001。
- 独立検証: 未実施（レビュー待ち）。
- 導入したGoal: G001。

## 確定した結果

研究担当の自足的証明としてC001、C002を保存した。独立レビューで受理された結果はまだない。

## 計算によって確認された結果

- 決定的DFSの50万候補内で得られた最大の証明書は8頂点・10辺。
- 同じ予算での再実行結果が `search-result.json` とバイト単位で一致。
- verifierの成功、両禁止構造の拒否、不正入力の拒否をテスト済み。
- 探索した範囲内の観察を全グラフについての不存在主張には昇格しない。

## 予想・未検証の主張

上界、正確な値、保存グラフの最大性について新しいClaimは導入していない。

## 反証された仮説・失敗した方針

一般的な頂点追加DFSを50万候補まで進めたが、保存された8頂点より大きな証明書は得られず、探索木も完了しなかった。
失敗した別方式や反証された数学的仮説はない。同じ予算・順序の再探索で新しい成果は出ない。
次に計算探索を行うなら、同型重複の簡約や探索順序の変更が必要だが、まず独立レビューを優先する。

## 既知情報による汚染

G001開始時（2026-09-06）、研究担当モデルが既知値 `9` と、8頂点のサイクルに対頂点間の辺を加える既知構成を意図せず想起した。完全な知識遮断ではない。これらは探索の入力・停止頂点数・対称性制約・証明の根拠に利用しない。空グラフから一般的な頂点追加探索を行い、得られたedge listを直接検証する。

## 現在のフロンティア・残っているケース

- 下界: R(3,4)≥9（研究担当の証明あり、独立レビュー待ち）。
- 上界: このリポジトリでは未証明。
- 未解決: 9頂点以上の一般のグラフについての回避可能性。予算打切りのため不存在の結論はない。

## 現在のボトルネック

G001自体の必須成果物は揃った。次はC001/C002の独立監査と、上界を得る自足的な議論の構築。

## 再開手順と次の課題

`verification.md` のコマンドで証明書・探索の再現を確認し、`proof.md` を読む。
独立ReviewerはC001/C002と対象コミットを明示して監査し、結果を `verification.md` に追記する。
次の研究Goalはレビュー結果を踏まえて上界証明を対象に設計する（本セッションでは新Goalを作成していない）。
