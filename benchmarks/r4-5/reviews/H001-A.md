# H001-A — independent review

## Scope and verdict

- Review date: 2026-09-22
- Reviewer: ChatGPT / GPT-5.6 Sol
- Reasoning effort: High
- PR: #15
- Execution base: `6031a203863a71cae5a34793c5d87dbc9b24479b`
- Immutable implementation/config/test commit: `5cabb7e2ea0f9ccbd237674666031d7d9f67ae3c`
- Research artifact commit: `4189237ad41e171d20a972da09d999263e130cc8`
- Pre-review PR head: `995a89beb5cbcb5acc2af5776af24e7fc460d79b`
- Discovery: `CONTAMINATED`

Verdict:

- **R45-C001: PROVEN × ACCEPTED** — the saved 23-vertex graph has no `K4` and no independent 5-set, hence `R(4,5) >= 24`.
- **H001-A acceptance criteria: PASS** — Phase A produced the required unfinished checkpoint at exactly 2,000,000 research candidates and stopped.
- **H001 overall: PARTIAL_PROGRESS** — Phase B has not run; cross-session handoff PASS/FAIL is not yet decided.
- Phase A termination remains `PARTIAL_PROGRESS / MANDATORY_HANDOFF_CHECKPOINT` by design, not `SOLVED` or `EXHAUSTED_BUDGET`.

This is the benchmark's independent review record, not an official GitHub APPROVE action.

## Independent mathematical verification

The reviewer checked `h001-a/run/certificate.json` with a standalone enumeration independent of `search.py` and the research-side verifier implementation.

Recorded reviewer environment:

- Python 3.13.5
- Linux 6.18.44 x86_64
- standard library only

Results:

- vertices: 23
- edges: 114
- all 4-subsets checked: `C(23,4)=8,855`
- `K4` count: 0
- all 5-subsets checked: `C(23,5)=33,649`
- independent 5-set count: 0
- triangle count: 125
- independent 4-set count: 153
- clique number: 3
- independence number: 4
- canonical certificate SHA-256: `5e5f6b18a032f0be038a83c69a39ae4f42755432dd36e8f30d43931fb6aad498`

The canonical digest matches the last `certificate_sha256` embedded in the Phase A checkpoint. Therefore the finite certificate independently supports `R(4,5)>23`, equivalently `R(4,5)>=24`.

Saved reviewer artifacts:

- `review/r45-h001-a-independent.py`
- `review/r45-h001-a-independent-result.json`

## Checkpoint and interval audit

The Phase A checkpoint records:

- consumed research interval: `[0,2,000,000)`
- next candidate index: `2,000,000`
- last completed candidate index: `1,999,999`
- target: `n=24`
- current score: 40
- best score: 4
- restart: 152
- iteration: 2,439
- current-target evaluations: 1,522,439
- PRNG state: `7431988031815036543`
- remaining interval: `[2,000,000,10,000,000)`
- Phase B research evaluations: 0

The completed-target counts sum to 477,561 evaluations through the `n=23` certificate. The live target satisfies

`152 * 10,000 + 2,439 = 1,522,439`

and

`477,561 + 1,522,439 = 2,000,000`.

Thus the global, target, restart and iteration counters agree exactly at the mandatory boundary. The progress log also terminates at 2,000,000 with target 24/current score 40/best score 4.

The checkpoint raw-file SHA-256 is

`fd295fd8595c2eff2e4800f2862e7bc61ca54d72c3d7c421a61688f26e43b3a3`

and its semantic state SHA-256 is

`e93c2555c8ef11e173ea2a8c1357b09052f004716babc4e1cea68be95d8a051a`.

## Resume mechanism audit

The reviewer inspected the state-machine semantics and the saved pre-research tests.

`test_search.py` compares the entire semantic state dictionary, plus canonical state bytes, for uninterrupted versus split/serialize/load/resume execution. No semantic state field is excluded. Saved tests cover:

1. a normal split (`111 + 157`),
2. an initialization/target-advance boundary (`1 + 1`),
3. a restart boundary (`7 + 1`).

The saved test record reports 556 explicitly labeled TEST/REPLAY candidate transitions, outside the research counter, and all three exact-state comparisons PASS. It also records 11,136 delta-versus-full-score checks, deterministic PRNG arithmetic and negative integrity controls.

The checkpoint loader validates schema, algorithm, config/code hashes, semantic-state hash, certificate hashes, graph scores, completed-target sequence and counter invariants without calling a research transition. `resume` additionally requires the manifest checkpoint hash, Phase A identity and the exact 2,000,000 boundary before continuing to 10,000,000.

The reviewer did **not** execute H001-B, did **not** replay the two million Phase A research candidates, and does not treat the small split tests as proof that the separate fresh-session handoff has already succeeded. That is the purpose of H001-B.

## Discovery and provenance

Git chronology is suitable for audit:

1. `6031a203` — H001-A execution base / merged Stage 4 definition
2. `e387f94e` — recalled possible value and accidental methodology exposure recorded before design
3. `f03bb70e` — candidate semantics and exact-resume plan fixed
4. `5cabb7e2` — implementation/config/tests fixed before research
5. `4189237a` — exact 2,000,000-candidate checkpoint and mathematical artifacts
6. `995a89be` — PR submission and phase-stop measurement record

The possible remembered value `R(4,5)=25` is not used as evidence, target horizon or stopping condition. Discovery therefore remains **CONTAMINATED** while correctness is independently reviewable.

## Important integration requirement

For this handoff experiment, PR #15 should be integrated with a **normal merge commit**, not squash or rebase merge.

Reason: the machine-readable checkpoint deliberately names immutable pre-research `code_commit = 5cabb7e2...`, and `h001-a/audit.py` verifies those exact historical bytes with `git show <code_commit>:...`. Preserving the Phase A commit ancestry makes that audit robust in a fresh clone even after the feature branch is deleted. Squashing/rebasing would undermine that source-of-truth invariant unless the protocol were revised first.

After merge, before H001-B begins, the integrator should record a PR-based integration receipt containing at least:

- PR #15 merge SHA and timestamp,
- confirmation that `5cabb7e2...` is still an ancestor/reachable commit,
- confirmation that the merged checkpoint SHA remains `fd295fd8...`,
- Phase B execution-base prerequisite and remaining interval.

H001-B must then start in a new fresh session from the merged `main` state. No Phase B candidate should be consumed before its startup audit.

## Non-claims

This review does not assert:

- an `n=24` nonexistence result,
- an upper bound for `R(4,5)`,
- the exact value of `R(4,5)`,
- completion of H001,
- successful cross-session resume before H001-B actually runs.

The current `n=24` best score 4 is heuristic search state only.