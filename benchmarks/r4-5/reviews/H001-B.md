# H001-B — independent review

## Scope and verdict

- Review date: 2026-09-22
- Reviewer: ChatGPT / GPT-5.6 Sol
- Reasoning effort: High
- PR: #17
- Phase B execution base: `e1299901c3533ce1f6843c425ff8c458b834facc`
- Pre-research audit/observer and executing commit: `d1c5cdf7e1dfa3a8750f6aaaaab7fc99248203c3`
- Research artifact commit: `f35b7f445a65b44dba6d22abd3577e4a02d48a58`
- Pre-review PR head: `7a2330f5df9498f168cba32275c2ececf7c66805`
- Discovery: `CONTAMINATED`

Verdict:

- **H001-B operational handoff: PASS × ACCEPTED**.
- **H001-B goal: SOLVED, independently accepted**.
- **Overall H001 checkpoint-resume experiment: operationally SOLVED / PASS accepted**.
- **R45-C001 remains PROVEN × ACCEPTED** — `R(4,5) >= 24`.
- No new mathematical Claim was established in Phase B.
- The mathematical `R(4,5)` workload remains **PARTIAL_PROGRESS**; no upper bound or exact value is claimed.

This is the benchmark's independent review record, not a GitHub official APPROVE action.

## Fresh-session and Git-only reconstruction

The saved goal-service objective contains only the repository URL, `benchmarks/r4-5/goals/H001-B.md`, and the instruction to use Git as source of truth. The Phase B record states that no Phase A conversation was supplied and no extra user question was required.

The execution base `e1299901...` contains the normal-merge integration of H001-A and its subsequent integration receipt. Before any Phase B research transition, the researcher saved Git provenance/discovery records and then committed the startup audit/observer as `d1c5cdf7...`.

The fresh-session property is a recorded session attestation rather than a cryptographic proof about hidden context. This limitation is explicitly preserved and does not weaken the verifiable Git-state portion of the handoff test.

## Checkpoint integrity and state reconstruction

Before resume, the startup audit validated all 26 Phase A manifest artifacts against both the merged Git base and the handoff manifest. It recovered:

- Phase A checkpoint SHA-256: `fd295fd8595c2eff2e4800f2862e7bc61ca54d72c3d7c421a61688f26e43b3a3`
- Phase A semantic state SHA-256: `e93c2555c8ef11e173ea2a8c1357b09052f004716babc4e1cea68be95d8a051a`
- next candidate index: `2,000,000`
- remaining interval: `[2,000,000,10,000,000)`
- target: `n=24`
- score / best score: `40 / 4`
- restart / iteration: `152 / 2,439`
- target evaluations: `1,522,439`
- PRNG state: `7431988031815036543`

No semantic checkpoint field was missing or ambiguous. The audit itself consumed zero research candidates.

## Code/config continuity

The reviewer compared the immutable search source at the Phase A code commit `5cabb7e2...` with the source at the Phase B executing commit `d1c5cdf7...`. Git reports the same blob for `search.py` (`73e8c5d57462f5065d3eb8dd1a92bc5785ff9f18`), and the recorded raw SHA-256 remains `d5ac3a9772b3b92fe5276013ed16fba05de25808e02b81200d9c5a30e4b7940b`. `verify.py` and the Phase A config hashes are also unchanged.

The config remains `integer-noisy-edge-flip-v1`, seed `20260922`, start order 5, restart interval 10,000. No search-semantic change was made before or during resume.

## Exact first resumed transition

`run_resume.py` was committed before research. It invokes the unchanged `search.py` CLI through `runpy` with the resolved manifest arguments. A profiling callback triggers on entry to the exact `step` function in that source file, disables itself before the step body executes, verifies the complete incoming state digest, and records the candidate index without changing state or PRNG.

The saved first-entry record is:

- first resumed candidate index: `2,000,000`
- incoming state SHA-256: `e93c2555c8ef11e173ea2a8c1357b09052f004716babc4e1cea68be95d8a051a`
- observer semantic mutations: 0

This anchors Phase B to the exact Phase A state `S(2,000,000)` before the first resumed candidate is consumed.

## Interval and duplicate-work audit

The unchanged state machine has two decisive properties:

1. `advance(s,c,count)` calls `step` exactly `count` times.
2. each `step` increments `next_candidate_index` exactly once.

The resume path validates the Phase A checkpoint/hash, requires `phase=H001-A` and `next_candidate_index=2,000,000`, then sets the fixed endpoint to 10,000,000. It advances in blocks of at most 100,000 until that endpoint.

The saved run contains exactly 80 consecutive progress rows from 2,100,000 through 10,000,000. The final checkpoint records:

- Phase B research interval: `[2,000,000,10,000,000)`
- Phase B candidate evaluations: `8,000,000`
- combined consumed interval: `[0,10,000,000)`
- final next candidate index: `10,000,000`
- restart / iteration: `952 / 2,439`
- target evaluations at n=24: `9,522,439`

The arithmetic is internally exact:

`952 * 10,000 + 2,439 = 9,522,439`

and the completed targets n=5..23 consume 477,561 evaluations, so

`477,561 + 9,522,439 = 10,000,000`.

Because the input state is anchored at S(2,000,000), the deterministic code/config are unchanged, the one research launch advances exactly eight million single-index transitions, and no search reconstruction/restart is performed, the repeated Phase A research candidate count is 0 and the gap count is 0.

## Split/resume evidence

Phase B reran the preregistered mechanism tests outside the research budget. The saved output is identical to Phase A:

- normal split: `111 + 157`
- target-advance / initialization boundary: `1 + 1`
- restart boundary: `7 + 1`
- every semantic state field and canonical state bytes equal
- 556 separately labeled TEST/REPLAY candidate transitions
- 11,136 delta-versus-exhaustive comparisons
- PRNG, tamper-rejection and invalid-certificate controls pass

The reviewer did not replay the two million Phase A research candidates or run a separate uninterrupted ten-million-candidate trajectory. The protocol requires the small exact-resume equivalence tests plus auditable cross-session continuation; it does not require a duplicate full trajectory. This limitation is recorded rather than overclaimed.

## Final state and mathematics

Final checkpoint:

- raw SHA-256: `10702e9992f823cfc18135fc68537ca7171506c1f6382b9005a7c87426ee05f4`
- semantic state SHA-256: `de0b95aa593d14e63b48fc85786175ad5dd13eff5447f86841dc532346190bff`
- target n=24
- current score 27
- best score 4
- PRNG state `17278674351399343217`

No n=24 certificate was found. All 19 certificates n=5..23 were carried forward unchanged. The strongest n=23 certificate is byte-identical to the already independently accepted Phase A artifact, so R45-C001 remains `PROVEN × ACCEPTED` without a new mathematical dependency.

The n=24 best score 4 and search failure are heuristic observations only. They provide no nonexistence result, upper bound, or exact value for `R(4,5)`.

## Measurement and provenance limits

The record gives 310.815747 seconds from the second-resolution goal-service start timestamp to first research-step entry, and 107.658952708 seconds for Phase B search plus final serialization/validation. The actual model ID and reasoning effort are `missing`; the recommended values are not substituted. Goal-service token counters are accounting units, not generated-token or monetary-cost measurements.

Discovery remains `CONTAMINATED`; the inherited remembered possible value and Git-source exposures were disclosed before research and did not change the already fixed continuation algorithm.

## Integration requirement

PR #17 must be integrated using a **normal merge commit**, not squash or rebase. The final checkpoint names `code_commit=d1c5cdf7...`, and the saved audit verifies historical Phase B source artifacts with `git show <code_commit>:...`. Preserving this ancestry keeps the audit valid after branch deletion.

After merge, the integrator should record a PR-based `results/r45-h001-b-integration-receipt.md` containing the PR #17 merge SHA/time, reachability of `d1c5cdf7...`, the final checkpoint SHA above, and the accepted operational verdict. That post-merge receipt is not part of the present review verdict.
