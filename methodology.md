# Ramsey Benchmark — Methodology

## 1. 目的

このリポジトリの目的は、既知のRamsey数を題材として、

> **CodexとChatGPTを組み合わせた数学研究ワークフローが、未知のRamsey数の研究にも利用可能か**

を検証することである。

単に既知の答えを再現することは目的としない。

評価対象は、AIが次の研究サイクルをどの程度自律的かつ正確に実行できるかである。

[
\text{探索}
\rightarrow
\text{構造の発見}
\rightarrow
\text{予想・補題}
\rightarrow
\text{証明}
\rightarrow
\text{独立検証}
\rightarrow
\text{次の探索}
]

最終的には、この方法論を未解決・未確定のRamsey数の研究へ適用できるかを判断する。

---

## 2. 基本方針

### 2.1 CodexとChatGPTの役割を分離する

原則として役割を次のように分ける。

### Codex — Researcher

Codexは主に研究を実行する。

* 反例・critical coloringの探索
* Python、SAT、SMT、整数計画等による計算
* 構造的特徴の抽出
* 補題・恒等式の発見
* 証明候補の構築
* ケース分類
* 失敗した仮説の反証
* 研究ノートの更新
* 証明本文の作成
* 機械検算

長時間の探索が必要な場合は`/goal`等を利用する。

### ChatGPT — Reviewer / Research Director

ChatGPTは主に研究結果の独立評価と次の方針決定を行う。

* Codexが作成した証明の独立監査
* 論理的飛躍・未証明仮定の検出
* 計算結果と数学的証明の区別
* 研究全体の現在地の整理
* ボトルネックの特定
* 次にCodexに与える研究課題の設計
* 複雑な証明の人間向け説明
* トークン消費に対する研究効率の評価

原則として、ChatGPTはCodexの会話履歴や自己評価ではなく、**Gitリポジトリに保存された成果物をsource of truthとして評価する。**

### 2.2 統括・通常のGoal設計の標準設定

2026-09-06の運用方針として、**統括・通常のGoal設計はChatGPTのSol × highに任せる。**

* Gitに保存された確定事項・未解決点・検証記録から研究の現在地を整理する。
* 次に取り組む課題の優先順位と資源配分を決める。
* Codexに渡すGoalの対象、成果物、検証条件、予算を具体化する。
* Goal終了後の成果を確認し、次のGoalを設計する。

この設定は統括・通常のGoal設計に適用する。研究実行、戦略の重点レビュー、独立検証に用いるモデルと推論レベルは別途決定する。

---

## 3. 情報汚染の防止

ベンチマークでは、可能な限り既知解からの情報流入を防ぐ。

各問題についてCodexに最初に与える情報は`problem.md`に固定する。

原則としてCodexには以下を与えない。

* Ramsey数の既知の正確な値
* 既知のextremal / critical graph
* 既知証明
* 既知証明で使われる主要補題
* 解説記事や既知解への誘導

ただし、Ramsey数の定義や一般的なグラフ理論、組合せ論、線形代数等の標準知識は利用してよい。

Codexが自身の事前学習から既知結果を想起する可能性は完全には排除できない。

そのため本ベンチマークは、

> **完全な知識遮断実験ではなく、既知証明を明示的に与えずに研究プロセスを再構築できるか**

を測定するものとする。

Codexが既知値や既知構成を明示的に記憶していると判断した場合は、その事実を記録する。

---

## 4. 問題ごとの研究フェーズ

各benchmarkは原則として以下のフェーズを通す。

### Phase A — Problem Setup

`problem.md`に対象問題と許可される手段を記述する。

最低限、以下を明示する。

* Ramsey数の定義
* 対象となるパラメータ
* 計算機利用の可否
* 最終成果物の要件
* 既知結果を検索してよいか

基本設定では、既知解の検索は禁止する。

---

### Phase B — Computational Exploration

Codexは必要に応じて計算機探索を行う。

例：

* brute force
* local search
* simulated annealing
* tabu search
* SAT / SMT
* CP-SAT
* integer programming
* graph generation
* symmetry breaking

計算結果は証明と区別して記録する。

特に、

> 「探索して見つからなかった」

ことだけを不存在証明として扱ってはならない。

一方で、条件を満たす具体的なグラフが発見された場合、それ自体が下界の有限証明書になり得る。

---

### Phase C — Structural Discovery

探索結果から数学的構造を抽出する。

例：

* 次数分布
* 共通近傍数
* 正則性
* 強正則性
* 対称性
* 飽和辺
* 局所グラフ
* 固有値
* orbit構造
* block design的構造

計算から観察された特徴は、まだ「予想」として扱う。

---

### Phase D — Proof Construction

観察された特徴を一般化し、人間可読な証明へ変換する。

可能な限り、

* 二重計数
* 鳩ノ巣原理
* 次数評価
* 包含排除
* 線形代数
* 固有値
* 正定値性
* 組合せ的不等式

などの自足的な議論を優先する。

有限全探索そのものを最終証明として認める場合には、探索プログラム、入力条件、完全性の根拠、検証方法を明記する。

---

### Phase E — Independent Verification

証明を作成した研究プロセスとは別に検証を行う。

可能なら以下のうち複数を実施する。

* ChatGPTによる独立レビュー
* 別導出による恒等式の再証明
* Python等による整数計算の検算
* SAT / SMTによる局所条件の検証
* 別実装による構成グラフの検査
* symbolic computation
* exhaustive verification
* proof assistant

重要な主張について、

[
\text{発見経路} \neq \text{検証経路}
]

となることを優先する。

---

## 5. 成果物

各benchmarkは原則として以下のファイルを持つ。

```text
benchmarks/<problem>/
├── problem.md
├── research-notes.md
├── proof.md
└── verification.md
```

### `problem.md`

Codexに最初に与える問題設定。

ベンチマーク開始後は原則として変更しない。

### `research-notes.md`

研究過程を記録する。

含めるもの：

* 発見した恒等式
* 補題候補
* 計算結果
* 反例候補
* 失敗した方針
* 排除されたケース
* 現在のボトルネック
* 次の研究課題

未証明の事項は明示的に「予想」「計算上確認」等と記載する。

### `proof.md`

最終的な証明のみを記載する。

研究履歴や失敗した試行は原則として含めない。

可能な限り、このファイル単独で証明を追える状態にする。

### `verification.md`

独立検証の記録。

* どの主張を検証したか
* どの方法で検証したか
* 証明とは独立した方法か
* 機械検算に依存するか
* 未検証部分が残っているか

を記録する。

---

## 6. 状態ラベル

研究ノートでは、各主張を少なくとも次の状態に分類する。

### `PROVEN`

自足的な数学的証明が存在する。

### `COMPUTATIONALLY VERIFIED`

有限計算等により確認されているが、人間可読な証明としては未完成。

### `CONJECTURE`

観察または探索から得られた予想。

### `REFUTED`

反例または矛盾によって否定された。

### `UNVERIFIED`

Codex等が主張したが、独立検証されていない。

最終的な`proof.md`では、原則として`PROVEN`の事項のみを利用する。

---

## 7. Codexの停止・再開

長時間タスクではトークン消費を抑えるため、研究を途中で停止してよい。

ただし、停止時には現在の研究状態をGit上に保存する。

最低限、以下を記録する。

1. 新たに証明できた事項
2. 計算でのみ確認できた事項
3. 否定された仮説・失敗した方針
4. 現在残っているケース
5. 現在のボトルネック
6. 次に試すべき具体的な方針

AIの内部コンテキストではなく、

[
\boxed{\text{Git repositoryを研究状態のsource of truthとする}}
]

ことを基本原則とする。

---

## 8. Goalの設計

巨大なGoalを一度に解かせ続けることを避ける。

原則として、

[
\text{大きな研究目標}
\rightarrow
\text{小さな検証可能な研究課題}
]

へ分割する。

良いGoalの例：

* 特定の次数ケースを完全分類する
* ある補題を証明または反証する
* critical coloringを一つ発見する
* 特定の探索空間が空であることを検証する
* 一つのボトルネックを解消する

避けるべきGoalの例：

* 「このRamsey数を何としても解け」
* 「証明が完成するまで考え続けろ」

各Goalの終了時には、成果をGitに固定してから次のGoalを決定する。

---

## 9. ChatGPTレビュー

Codexの研究成果が一定量蓄積した段階でChatGPTにレビューを依頼する。

ChatGPTは原則として以下を確認する。

1. 各主張は本当に前提から導かれているか
2. 必要条件を十分条件として扱っていないか
3. 計算上の観察を証明として扱っていないか
4. 全ケースを尽くしたことが本当に示されているか
5. 対称性やWLOGの使用は正当か
6. 整数性・偶奇性・境界条件に見落としがないか
7. 証明の依存関係に循環がないか
8. より単純な証明に圧縮できないか
9. 次の研究資源をどこへ投入すべきか

重大な結果については、ChatGPTに「証明する立場」ではなく**反証する立場**から監査させる。

---

## 10. ベンチマーク評価指標

単純な正解率だけでは評価しない。

各問題について可能な範囲で以下を記録する。

### Correctness

* 正確なRamsey数へ到達したか
* 下界構成は正しいか
* 上界証明は正しいか
* ChatGPTの監査を通過したか

### Discovery

* 既知構成を明示的に与えず発見できたか
* 重要な補題を自力で発見できたか
* 誤った仮説を自ら反証できたか

### Efficiency

* Codex Goalの回数
* 大まかなトークン消費
* 計算時間
* 不要な探索の量
* ChatGPTレビュー後に必要となった修正回数

### Proof Quality

* 人間可読性
* 証明の長さ
* 計算依存度
* 独立検証可能性
* 既知証明との差異

### Research Process

* 計算と理論を有効に往復できたか
* 行き詰まりを正しく特定できたか
* ChatGPTのレビューが研究方向を改善したか
* Goal分割により研究効率が改善したか

---

## 11. 段階的ベンチマーク

初期ベンチマークとして以下を想定する。

### Stage 1 — (R(3,4))

目的：

* 基本的なCodex → Git → ChatGPTのサイクル確認
* 下界構成の発見
* 短い上界証明の再発見
* 独立レビューの成立確認

### Stage 2 — (R(3,5))

目的：

* より大きな探索空間への対応
* 計算探索と理論の往復
* Goal分割の効果測定

### Stage 3 — (R(4,4))

目的：

* 本格的な構造分類
* 長時間研究におけるコンテキスト管理
* CodexとChatGPTの役割分担の限界確認

Stage 1から順に実施し、前段階で方法論上の問題が判明した場合は、次へ進む前に改善する。

---

## 12. 最終的に検証したい仮説

本ベンチマークで検証したい中心仮説は次である。

> **Codexに長時間の探索・証明発見を担当させ、ChatGPTに研究方向の決定と独立レビューを担当させることで、単一のAIセッションよりも効率的かつ信頼性の高い数学研究が可能になる。**

さらに、

> **既知の小規模Ramsey数でこの研究サイクルが再現可能であれば、未確定Ramsey数に対しても同じプロセスを適用する合理的根拠になる。**

ただし、既知問題で成功したことは未知問題での成功を保証しない。

本ベンチマークの目的は保証ではなく、**AIを用いた数学研究プロセスの能力・限界・失敗パターンを事前に把握すること**にある。
